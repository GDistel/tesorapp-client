export enum ExpensesListStatus {
  OPEN = 'OPEN',
  ARCHIVED = 'ARCHIVED',
};

export enum Currencies {
  ARS = 'ARS',
  BRL = 'BRL',
  EUR = 'EUR',
  USD = 'USD'
};

export const enum ExpensesListsAction {
  add = 0,
  delete
}
